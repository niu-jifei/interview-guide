# 2026-07-29

## 调研：StructuredOutputInvoker 指标监控
- 审阅 `app/.../common/ai/StructuredOutputInvoker.java`：已埋 3 个 Micrometer 指标（invocations/attempts Counter + latency Timer，标签 context/status），由 `isMetricsAvailable()` 控制。
- 关键结论：指标当前「休眠」——`build.gradle` 无 `spring-boot-starter-actuator`、无 registry binder，`application.yml` 无 `management:` 块，故无 `MeterRegistry` Bean，`@Autowired(required=false)` 为 null。同目录 `VoiceInterviewWebSocketHandler` 与 `LlmProviderRegistry` 亦依赖 actuator 才能导出。
- 产出文档：`app/.../common/ai/结构化输出&指标监控.md`，给出 Prometheus+Grafana（推荐）/JMX/SaaS 三套落地方案及待确认待办。未改任何代码。
